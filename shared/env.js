import {
  API_BASE,
  WS_BASE,
  PUBLIC_FORCE_TURN_DEFAULT,
  getTurnAdminKey,
  setTurnAdminKey,
  buildApiUrl,
  buildWsUrl,
  readBooleanQueryFlag,
  readNumericQuery,
  ensureTurnKey,
  resetStoredTurnKey,
} from "../public/js/shared/env.js";

export {
  API_BASE,
  WS_BASE,
  PUBLIC_FORCE_TURN_DEFAULT,
  getTurnAdminKey,
  setTurnAdminKey,
  buildApiUrl,
  buildWsUrl,
  readBooleanQueryFlag,
  readNumericQuery,
  ensureTurnKey,
  resetStoredTurnKey,
} from "../public/js/shared/env.js";

export const ENV = {
  API_BASE,
  WS_BASE,
};
